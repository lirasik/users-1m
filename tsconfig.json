{
  "compilerOptions": {
    "target": "ES6", // Указывает версию JavaScript, в которую будет транслироваться TypeScript
    "module": "ESNext", // Использует ES-модули
    "jsx": "react-jsx", // Поддержка JSX для React
    "strict": true, // Включает строгий режим TypeScript
    "esModuleInterop": true, // Упрощает работу с CommonJS модулями
    "skipLibCheck": true, // Пропускает проверку типов в файлах библиотек
    "forceConsistentCasingInFileNames": true, // Требует одинаковый регистр в путях
    "moduleResolution": "node", // Разрешение модулей как в Node.js
    "resolveJsonModule": true, // Поддержка импорта JSON-файлов
    "isolatedModules": true, // Требует, чтобы каждый файл был модулем
    "noEmit": true // Отключает генерацию файлов JavaScript
  },
  "include": ["src"], // Указывает, что TypeScript должен обрабатывать файлы в папке src
  "exclude": ["node_modules"] // Исключает папку node_modules
}
